# agent-integration

## Setup
1. Install dependencies: `npm install`
2. Run tests: `npm test`
3. Build: `npm run build`
4. Start: `npm start`

## Post-merge process
After merging, run: `npm run postmerge`
